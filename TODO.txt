
commands:
	- mc_skin amount:1

- bundle commands together (e.g. .generate_4chan, .generate_discord, .set_chatbot_confidence, ...)
- .stream recommendations based on link (do in infinite loop)
- log more information about everything
- randomly evolve AI picture (pick best image)
- randomly text someone (or every x hours or every morning)
- volume when using .play
- playback speed when using .play
- shutdown bot when no internet